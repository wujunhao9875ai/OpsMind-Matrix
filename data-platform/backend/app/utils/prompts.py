"""Prompt 模板管理"""

# 素材生成 Prompt
MATERIAL_GENERATION_PROMPT = """你是一个运维知识工程师，负责从运维对话记录中提取高质量的问答对。

请根据以下对话内容，生成 {count} 个问答对。

规则：
1. 问题应该清晰、具体，模拟真实用户提问方式
2. 答案应该专业、准确，包含操作步骤或解决方案
3. 避免生成重复或相似度高的问答对
4. 确保答案基于对话中的知识，不要编造信息

对话内容：
{context}

请以 JSON 数组格式输出，每个元素包含 question 和 answer 字段。"""

# 质量评分 Prompt
QUALITY_SCORING_PROMPT = """请评估以下问答对的质量，从以下维度打分（1-10分）：
1. 问题清晰度：问题是否明确、无歧义
2. 答案准确性：答案是否专业、正确
3. 实用性：这个问答对的实际应用价值
4. 完整性：答案是否包含足够的信息

问答对：
问题：{question}
答案：{answer}

请以 JSON 格式输出评分结果。"""

# 数据清洗 Prompt
DATA_CLEANING_PROMPT = """请清洗以下文本，移除无关内容、修正格式：
{text}

规则：
1. 移除多余的空格和换行
2. 修正明显的拼写错误
3. 保持专业术语不变
4. 确保输出格式规范"""

# 数据集导出 Prompt
DATASET_EXPORT_PROMPT = """请将以下数据转换为 {format} 格式的训练数据集：
{data}

要求：
1. 保持 instruction-input-output 三元组格式
2. 确保 JSON 格式合法
3. 保留原始数据的语义信息"""